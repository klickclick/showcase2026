[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# Optional: Handle SPA routing if we add it later
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
